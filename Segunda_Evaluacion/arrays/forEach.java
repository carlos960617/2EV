
        /*No necesita indices, es mas sencillo pero no siempre se utiliza.

        for (tipoDato nombreValorDelArray:nombreDelArray ){

        }

        */

        //Siempre itera hacia delante no se puede usar length-1 por ejemplo ni dos condiciones

        /*Comparacion de arrays: Arrays.equals(array1, array2) tienen que tener la
         misma longitud y estar en el mismo orden
         tambien hay Array.toString, array.sort (los ordena de menor a myor)
    }   */

